

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Request {
  id      Int    @id @default(autoincrement())
  method  String
  url     String
  headers String
  body    String
}
